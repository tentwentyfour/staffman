{% extends "base.twig" %}

{% block title %} &middot; Services {% endblock %} {# title #}

{% block authentication %}

	<p class="navbar-text pull-right">
		<input type="button" class="btn btn-warning disabled" value="InfoButton" id="infobutton">
		<i class="icon-user"></i> Welcome <a href="#" class="navbar-link">{{ user.firstname }} {{ user.lastname }}</a>
	</p>
{% endblock %} {# authentication #}

{% block container %}
<!-- Jumbotron -->
  <!--<div class="jumbotron">
    <h1>Marketing stuff!</h1>
    <p class="lead">Cras justo odio, dapibus ac facilisis in, egestas eget quam. Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus.</p>
    <a class="btn btn-large btn-success" href="#">Get started today</a>
  </div>

  <hr>-->

    <div class="row">
        <div class="col-xs-12">
            <!--<div class="alert alert-warning"><strong>Mails</strong> | You have an autoreply answering all your mails!</div>-->
        </div>
        <div class="col-xs-4">
            <div class="panel panel-info">
                <div class="panel-heading">
                    <h3 class="panel-title"><a href="{{ config.maildomain }}" target="_blank">Mails</a></h3>
                </div>
                <div class="panel-body">
                    not yet implemented
                </div>
            </div>
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title"><a href="{{ config.maildomain }}" target="_blank">Calendar</a></h3>
                </div>
                <div class="panel-body">
                    not yet implemented
                </div>
            </div>
        </div>
        <div class="col-xs-4">
            <div class="panel panel-success">
                <div class="panel-heading">
                    <h3 class="panel-title"><a href="{{ config.redminedomain }}" target="_blank">Redmine</a></h3>
                </div>
                <div class="panel-body">
                    {% for commit in commits %}
                        <li class="redmine-commit">
                            <strong><a href="{{ config.redminedomain }}/projects/{{ commit.identifier }}" target="_blank">{{ commit.name }}</a></strong>
                            <a href="{{ config.redminedomain }}/projects/{{ commit.identifier }}/repository/revisions/{{ commit.revision }}" class="btn btn-info btn-xs">{{ commit.date|date("d M G:i", false) }}</a><br />
                            <strong>{{ commit.committer }}</strong> <i>{{ commit.comments }}</i>
                        </li>
                    {% endfor %}
                </div>
            </div>
        </div>
        <div class="col-xs-4">
            <div class="row">
                <div class="col-xs-6">
                    <a href="#" class="btn btn-block btn-info btn-lg">Bla</a>
                </div>
                <div class="col-xs-6">
                    <a href="#" class="btn btn-block btn-info btn-lg">Bla</a>
                </div>
            </div>
            <br />
            <div class="row">
                <div class="col-xs-6">
                    <button href="#" class="btn btn-block btn-info btn-lg btn-change-password-modal">Change Password</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %} {# container #}

